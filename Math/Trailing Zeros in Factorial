class Solution:
    # @param A : integer
    # @return an integer
    def trailingZeroes(self, A):
        zero =0
        p = 1
        while (A//(5**p) != 0):
            zero = zero + A//(5**p)
            p+=1
            
        return zero
